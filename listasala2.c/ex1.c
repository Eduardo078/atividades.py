#include <stdio.h>
int main(){
    float n1, n2, n3, , nm;
    m = (n1 + n2) / 2

    printf("digite a nota 1:");
    scanf("%f", &n1);
    printf("digite a nota 2:");
    scanf("%f", &n2);
    printf("digite a nota 3:");
    scanf("%f", &n3);

    if (m >= 6.0)
        printf("Aprovado, média: %f", m);
    else if (m < 6.0)
        printf("Reprovado, média: %f", m);
    return 0;
}